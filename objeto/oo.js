//CODIGO NO EJECUTABLE//TEORICO

//Procedural
// este tipo de codigo se vasa en el prosesamiento de datos, mediantes funciones
processamento(valor1, valor2, valor3)

// OO // paradigma orientacion a objetos
// se relaciona mas con el mundo, cada objeto tiene su cantidad de funciones, valores y objetos mismos como en el mundo real
// lo que pasa aca es que ahora tenemos objeto que contienen valores y funciones, el objeto pasa a tener un foco mucho mayor
objeto = {
    valor1,
    valor2,
    valor3,
    processamento(){
        // ...
    }
}
// objeto pasa a ser una especie de capsula

objeto.processamento() // foco pasa a ser el objeto en vez de la funcion

// Principios importantes:
// 1. abstracao // agarrar lo que es relevante de un objeto del mundo real para tu codigo, evitar las complejidades que no son relevantes o no aportan
// 2. encapsulamento // tener los detalles ocultados y solo mostrarlos en el momento que los necesite para interactuar con determinado objeto
// 3. herança (prototype) // tener objetos padre y objetos hijo donde el hijo resive algunos aspecto del objeto padre
// 4. polimorfismo // a partir de algo mas generico se le puede atribuir una variable mas especisifica a este mismo
